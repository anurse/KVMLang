#![feature(phase)]

extern crate docopt;

use docopt::Docopt;

static USAGE: &'static str = "
Usage: kvm help [options]
       kvm get-latest [options]

Options:
	-f <feed-url>, --feed <feed-url>
";

fn main() {
}
